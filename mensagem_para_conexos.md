# Mensagem para enviar ao suporte/gerente de conta do CONEXOS

Copie e envie o texto abaixo (por e-mail, chat de suporte, ou para o
seu gerente de conta no CONEXOS). O objetivo é descobrir se existe uma
forma de consultar os dados de status de processos de importação de
fora do sistema.

---

Olá,

Somos clientes do CONEXOS na SeletoComex e estamos desenvolvendo um
assistente automatizado (bot) para responder nossos clientes sobre o
status/andamento (followup) das importações deles via WhatsApp.

Para isso, gostaríamos de entender:

1. O CONEXOS oferece uma **API REST** (ou similar) para consulta de
   status de processos de importação (número do processo, container,
   BL, status atual, previsão de chegada/entrega, histórico de eventos)?

2. Existe algum **webhook** que nos avise automaticamente quando o
   status de um processo muda?

3. Se não houver API/webhook, existe alguma forma de **exportação
   automatizada** (ex: relatório periódico via e-mail/FTP, integração
   com planilha) que possamos usar como alternativa?

4. Caso exista API, poderiam nos enviar a **documentação técnica**
   (endpoints, autenticação, exemplos de requisição/resposta) e nos
   informar se há custo adicional para uso da integração?

Ficamos no aguardo. Desde já, agradecemos o apoio.

Atenciosamente,
Bernardo Mello — SeletoComex

---

**Depois de receber a resposta:** me envie a documentação (PDF, link,
Postman collection, capturas de tela — o que tiver) e eu implemento a
integração real em `conexos_connector.py`, sem precisar tocar no resto
do bot.
